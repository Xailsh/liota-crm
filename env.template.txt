# ============================================================
# LIOTA CRM — Environment Variables for Railway Deployment
# ============================================================
# On Railway: go to your project → Variables tab → add each one below.
# For local dev: rename this file to .env and fill in the values.

# ─── Database ────────────────────────────────────────────────
# MySQL connection string.
# On Railway: add a MySQL plugin → it auto-fills DATABASE_URL.
DATABASE_URL=mysql://user:password@host:3306/liota_crm

# ─── Auth / Session ──────────────────────────────────────────
# Random secret for signing session JWTs. Generate with:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_64_CHAR_RANDOM_HEX

# Manus OAuth — only needed for "Sign in with Manus" button.
# Staff email+password login works WITHOUT these. Leave blank to disable.
VITE_APP_ID=
OAUTH_SERVER_URL=
VITE_OAUTH_PORTAL_URL=
OWNER_OPEN_ID=

# ─── Google OAuth (staff Google sign-in) ─────────────────────
# Create at: https://console.cloud.google.com/apis/credentials
# Redirect URI: https://YOUR_RAILWAY_DOMAIN/api/staff-auth/google/callback
GOOGLE_CLIENT_ID=REPLACE_WITH_GOOGLE_CLIENT_ID
GOOGLE_CLIENT_SECRET=REPLACE_WITH_GOOGLE_CLIENT_SECRET

# ─── Email (Resend) ──────────────────────────────────────────
# Sends drip emails, lead notifications, invitations.
# Get key at: https://resend.com/api-keys
RESEND_API_KEY=re_REPLACE_WITH_RESEND_KEY

# ─── Meta / Facebook Lead Ads ────────────────────────────────
# Any random string — enter the same value in Meta for Developers
# under Webhooks → Verify Token when setting up the webhook.
META_WEBHOOK_VERIFY_TOKEN=REPLACE_WITH_RANDOM_STRING

# ─── AI Chat (LLM) ───────────────────────────────────────────
# The AI Chat widget calls an OpenAI-compatible API.
# Use your OpenAI key directly, or any compatible provider.
BUILT_IN_FORGE_API_URL=https://api.openai.com
BUILT_IN_FORGE_API_KEY=sk-REPLACE_WITH_OPENAI_KEY

# ─── App URL ─────────────────────────────────────────────────
# Your Railway public domain — used in email links and webhooks.
APP_URL=https://your-app.up.railway.app
